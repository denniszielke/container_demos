apiVersion: v1
kind: Service
metadata:
  name: colorservice
  labels:
    name: colorservice
spec:
  selector:
    name: servicedemo 
  type: NodePort
  ports:
   - port: 80
     name: http
     targetPort: 3000
     protocol: TCP