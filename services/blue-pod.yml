apiVersion: "v1"
kind: Pod
metadata:
  name: greendemo
  labels:
    name: servicedemo
    color: green
    demo: colorsdemo
spec:
  containers:
    - name: blue
      image: hellodemodz234.azurecr.io/blue:latest
      ports:
        - containerPort: 80
          name: http
          protocol: TCP 
  imagePullSecrets:
    - name: kuberegistry